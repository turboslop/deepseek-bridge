interface:
  display_name: "Issue to Green PR"
  short_description: "Repo workflow for taking an issue through green PR checks and auto-merge."
  default_prompt: "Take an issue from this repository through branch, implementation, validation, PR creation, green CI, and auto-merge."
